## Ejecucion

### Activar el entorno virtual 
en la terminal ejecutar 'poetry env activate'
y luego copiar y ejecutar el comando que te devuelve. 

### Correr
Desde raiz de el proyecto ejecutar : 'uvicorn app.main:app --reload'
